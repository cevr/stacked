---
"@cvr/stacked": patch
---

Improve trunk auto-detection by preferring the repo default branch from `origin/HEAD`, falling back to common local branch names, and align onboarding/docs to present manual trunk configuration as an override rather than a required setup step.
